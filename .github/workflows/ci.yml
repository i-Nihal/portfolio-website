name: CI Portfolio Check
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check files
        run: ls -la
      - name: Say Hello
        run: echo "Great job on the portfolio!"